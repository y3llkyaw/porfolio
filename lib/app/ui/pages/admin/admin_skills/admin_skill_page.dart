import 'package:flutter/widgets.dart';

class AdminSkillPage extends StatelessWidget {
  const AdminSkillPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Admin Skill Page'),
      ],
    );
  }
}
