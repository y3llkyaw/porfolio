import 'package:flutter/widgets.dart';

class AdminEducationPage extends StatelessWidget {
  const AdminEducationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Admin Education Page'),
      ],
    );
  }
}
